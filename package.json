{
  "name": "aiship-child",
  "version": "1.0.0",
  "private": true,
  "description": "Divi Child Theme for aiship.co",
  "scripts": {
    "lint": "npm run lint:css && npm run lint:js",
    "lint:css": "stylelint 'assets/css/**/*.css' 'style.css'",
    "lint:js": "eslint 'assets/js/**/*.js'",
    "lint:fix": "npm run lint:css -- --fix && npm run lint:js -- --fix"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^37.0.0"
  }
}
